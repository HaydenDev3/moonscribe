import { useMemo, useState, type CSSProperties } from 'react'
import Icon from '../components/Icon'
import { safeWebsiteUrl, type AuthorWebsite, type WebsiteBook } from './model'

const NAV=[['home','Home'],['books','Books'],['about','About'],['journal','Journal'],['contact','Contact']]
const THEMES={moonlight:'bg-[#080b0e] text-[#f2eadf] [--surface:#0d1216] [--muted:#a8a29a] [--line:rgba(255,255,255,.13)]',parchment:'bg-[#ede2ce] text-[#32251d] [--surface:#f5ecdd] [--muted:#705e50] [--line:rgba(59,43,32,.2)]',ember:'bg-[#100b09] text-[#f3e7dc] [--surface:#1a110e] [--muted:#b49c8b] [--line:rgba(229,164,111,.2)]',midnight:'bg-[#060910] text-[#e8e6df] [--surface:#0b111c] [--muted:#9ca7b7] [--line:rgba(185,168,117,.2)]'}
const SPACE={compact:'py-8 md:py-10',comfortable:'py-10 md:py-14',spacious:'py-14 md:py-20'}
const jump=(id:string)=>document.getElementById(id)?.scrollIntoView({behavior:'smooth'})
const icon=(kind?:string)=>kind==='instagram'?'fa-brands fa-instagram':kind==='x'?'fa-brands fa-x-twitter':kind==='youtube'?'fa-brands fa-youtube':kind==='github'?'fa-brands fa-github':'fa-solid fa-link'

export default function AuthorSite({site,compact=false,aboutOnly=false}:{site:AuthorWebsite;compact?:boolean;aboutOnly?:boolean}){const [menu,setMenu]=useState(false);const socials=site.links.filter(l=>l.kind&&!['contact','website'].includes(l.kind));const style={'--accent':site.accent,'--site-bg':site.background} as CSSProperties;return <div style={style} className={`author-site-root ${compact?'author-site-compact':''} min-h-full overflow-x-hidden ${THEMES[site.theme]} ${site.typography==='modern'?'font-sans':'font-serif'}`}><SiteHeader site={site} socials={socials} menu={menu} setMenu={setMenu} compact={compact}/>{aboutOnly?<AboutPage site={site}/>:<><Hero site={site}/><Books site={site}/><About site={site}/>{site.journalEnabled&&<Journal site={site}/>}</>}<Footer site={site} socials={socials}/></div>}

function SiteHeader({site,socials,menu,setMenu,compact=false}:{site:AuthorWebsite;socials:AuthorWebsite['links'];menu:boolean;setMenu:(v:boolean)=>void;compact?:boolean}){const desktopNav=!compact;return <header id="home" className="sticky top-0 z-40 border-b border-[var(--line)] bg-[color:var(--surface)]/95 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-[1180px] items-center gap-5 px-5 md:px-8"><button onClick={()=>jump('home')} className="flex min-w-0 items-center gap-3"><span className="text-3xl text-[var(--accent)]">☾</span><strong className="truncate text-lg font-normal md:text-xl">{site.authorName||'Your name'}</strong></button>{desktopNav&&<nav className="ml-auto hidden items-center gap-7 text-sm lg:flex">{NAV.map(([id,label])=>(id!=='journal'||site.journalEnabled)&&<button key={id} onClick={()=>jump(id)} className="border-b border-transparent py-2 text-[var(--muted)] transition hover:border-[var(--accent)] hover:text-inherit">{label}</button>)}</nav>}{desktopNav&&<div className="hidden items-center gap-3 md:flex">{socials.slice(0,4).map(l=><a key={l.id} href={safeWebsiteUrl(l.url)} aria-label={l.label} className="text-[var(--muted)] hover:text-[var(--accent)]"><Icon icon={icon(l.kind)}/></a>)}<a href={safeWebsiteUrl(site.contactUrl)||'#contact'} className="ml-2 rounded-lg border border-[var(--accent)] px-5 py-2 text-sm text-[var(--accent)] hover:bg-[var(--accent)] hover:text-black">Follow</a></div>}<button className="ml-auto grid h-11 w-11 place-items-center text-2xl" onClick={()=>setMenu(!menu)} aria-expanded={menu} aria-label={menu?'Close navigation':'Open navigation'}><Icon icon={menu?'fa-solid fa-xmark':'fa-solid fa-bars'}/></button></div>{menu&&<div className="border-t border-[var(--line)] bg-[color:var(--surface)] px-5 py-5"><nav className="grid">{NAV.map(([id,label])=>(id!=='journal'||site.journalEnabled)&&<button key={id} onClick={()=>{jump(id);setMenu(false)}} className="rounded-lg px-4 py-3 text-left text-lg hover:bg-white/5">{label}</button>)}</nav><div className="mt-4 flex items-center gap-5 border-t border-[var(--line)] pt-5">{socials.map(l=><a key={l.id} href={safeWebsiteUrl(l.url)} aria-label={l.label}><Icon icon={icon(l.kind)}/></a>)}<a href={safeWebsiteUrl(site.contactUrl)||'#contact'} className="ml-auto rounded-lg border border-[var(--accent)] px-5 py-2 text-[var(--accent)]">Follow</a></div></div>}</header>}

function Hero({site}:{site:AuthorWebsite}){const bg=site.heroImage?{backgroundImage:`linear-gradient(90deg,rgba(0,0,0,.94),rgba(0,0,0,${site.heroOverlay/100}),rgba(0,0,0,.2)),url(${site.heroImage})`}:{backgroundImage:'radial-gradient(circle at 78% 36%,rgba(214,166,75,.24),transparent 22%),linear-gradient(125deg,#05080b 18%,#10202c 64%,#2d2117)'};return <section className="relative min-h-[560px] border-b border-[var(--line)] bg-cover bg-center" style={bg}><div className="absolute inset-0 bg-gradient-to-b from-transparent to-black/40"/><div className="relative mx-auto flex min-h-[560px] max-w-[1180px] items-center px-6 py-16 md:px-10 lg:py-24"><div className="max-w-2xl"><p className="mb-4 text-xs font-semibold tracking-[.24em] text-[var(--accent)]">{site.heroEyebrow}</p><h1 className="text-5xl font-normal leading-[.9] tracking-[-.035em] text-[#f5ead8] sm:text-6xl lg:text-8xl">{site.authorName||'Your name'}</h1><p className="mt-5 text-2xl italic text-[#f0dfc8] md:text-3xl">{site.tagline}</p><p className="mt-5 max-w-xl font-sans text-sm leading-7 text-white/70 md:text-base">{site.intro||site.bio}</p><div className="mt-7 flex flex-col gap-3 sm:flex-row"><button onClick={()=>jump('books')} className="rounded-lg bg-[var(--accent)] px-6 py-3 font-sans text-sm font-semibold text-[#17120b] shadow-xl hover:-translate-y-0.5">{site.primaryCta} <span className="ml-2">→</span></button>{site.journalEnabled&&<button onClick={()=>jump('journal')} className="rounded-lg border border-[var(--accent)] px-6 py-3 font-sans text-sm text-[#f2eadf] hover:bg-white/5">{site.secondaryCta}</button>}</div></div>{site.heroQuote&&<blockquote className="absolute bottom-8 right-8 hidden max-w-[220px] text-lg italic text-[var(--accent)] lg:block">{site.heroQuote}</blockquote>}</div></section>}

function Books({site}:{site:AuthorWebsite}){const books=useMemo(()=>[...site.books].sort((a,b)=>a.order-b.order),[site.books]);return <section id="books" className={`border-b border-[var(--line)] ${SPACE[site.sectionSpacing]}`}><div className="mx-auto max-w-[1180px] px-5 md:px-8"><div className="mb-5 flex items-end justify-between"><h2 className="text-3xl font-normal md:text-4xl">Featured Books</h2><span className="text-sm text-[var(--accent)]">View all books →</span></div>{books.length?<div className="-mx-5 flex snap-x snap-mandatory gap-4 overflow-x-auto px-5 pb-3 [scrollbar-width:none] md:mx-0 md:grid md:grid-cols-2 md:px-0 lg:grid-cols-3">{books.map(b=><BookCard key={b.novelId} book={b} site={site}/>)}</div>:<div className="rounded-2xl border border-dashed border-[var(--line)] bg-[color:var(--surface)] p-10 text-center font-sans text-[var(--muted)]">Select books in the builder to feature them here.</div>}</div></section>}
function BookCard({book,site}:{book:WebsiteBook;site:AuthorWebsite}){return <article className="grid w-[82vw] max-w-[360px] flex-none snap-start grid-cols-[105px_1fr] gap-4 rounded-xl border border-[var(--line)] bg-[color:var(--surface)] p-3.5 md:w-auto md:max-w-none"><div className="aspect-[2/3] overflow-hidden rounded-md bg-gradient-to-br from-slate-700 to-slate-950 shadow-xl">{book.cover?<img src={book.cover} alt={`${book.title} cover`} className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center text-3xl text-[var(--accent)]">☾</div>}</div><div className="flex min-w-0 flex-col py-2"><h3 className="text-xl leading-tight">{book.title}</h3>{site.showBookStatus&&book.status&&<span className="mt-2 font-sans text-[10px] uppercase tracking-[.18em] text-[var(--accent)]">{book.status}</span>}{site.showBookDescriptions&&book.description&&<p className="mt-2 line-clamp-3 font-sans text-xs leading-5 text-[var(--muted)]">{book.description}</p>}<a href={safeWebsiteUrl(book.url||'')||'#'} className="mt-auto rounded-md border border-[var(--accent)] px-3 py-2 text-center font-sans text-xs text-[var(--accent)]">Learn more <span className="ml-2">→</span></a></div></article>}

function About({site}:{site:AuthorWebsite}){const aboutHref=window.location.pathname.startsWith('/@')?`${window.location.pathname.split('/')[1]}/about`:'#about';return <section id="about" className={SPACE[site.sectionSpacing]}><div className="mx-auto grid max-w-[1180px] gap-7 px-5 md:grid-cols-[minmax(240px,.72fr)_1.3fr_.7fr] md:items-center md:px-8"><div className="aspect-[4/3] overflow-hidden rounded-2xl border border-[var(--line)] bg-gradient-to-br from-slate-700 to-slate-950">{site.profileImage?<img src={site.profileImage} alt={site.authorName} className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center text-7xl text-[var(--accent)]">☾</div>}</div><div><h2 className="text-3xl font-normal md:text-4xl">{site.aboutHeading}</h2><p className="mt-4 font-sans text-sm leading-7 text-[var(--muted)]">{site.aboutText||site.bio||`I'm ${site.authorName||'an author'}, a writer of mysterious worlds and the quiet moments in between.`}</p><a href={aboutHref} className="mt-5 inline-block text-sm text-[var(--accent)]">Read more about me →</a></div><ul className="grid gap-4 border-t border-[var(--line)] pt-6 font-sans text-sm text-[var(--muted)] md:border-l md:border-t-0 md:pl-8 md:pt-0">{site.interests.map((x,i)=><li key={`${x}-${i}`} className="flex items-center gap-3"><Icon icon={['fa-regular fa-bookmark','fa-solid fa-pen-nib','fa-regular fa-moon','fa-solid fa-compass'][i%4]}/><span>{x}</span></li>)}</ul></div></section>}
function AboutPage({site}:{site:AuthorWebsite}){return <main className="border-b border-[var(--line)]"><div className="mx-auto max-w-[1180px] px-5 py-16 md:px-8 md:py-24"><p className="font-sans text-xs font-semibold uppercase tracking-[.24em] text-[var(--accent)]">About the author</p><h1 className="mt-4 max-w-3xl text-5xl font-normal leading-none md:text-7xl">{site.authorName||'Your name'}</h1><p className="mt-6 max-w-2xl text-2xl italic text-[var(--accent)]">{site.tagline}</p><div className="mt-12 grid gap-10 md:grid-cols-[minmax(260px,.7fr)_1.3fr] md:items-start"><div className="aspect-[4/3] overflow-hidden rounded-2xl border border-[var(--line)] bg-gradient-to-br from-slate-700 to-slate-950">{site.profileImage?<img src={site.profileImage} alt={site.authorName} className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center text-8xl text-[var(--accent)]">☾</div>}</div><div><h2 className="text-3xl font-normal">{site.aboutHeading}</h2><p className="mt-5 max-w-2xl whitespace-pre-line font-sans text-base leading-8 text-[var(--muted)]">{site.aboutText||site.bio||`I'm ${site.authorName||'an author'}, a writer of mysterious worlds and the quiet moments in between.`}</p><ul className="mt-8 grid gap-4 border-t border-[var(--line)] pt-6 font-sans text-sm text-[var(--muted)] sm:grid-cols-2">{site.interests.map((x,i)=><li key={`${x}-${i}`} className="flex items-center gap-3"><Icon icon={['fa-regular fa-bookmark','fa-solid fa-pen-nib','fa-regular fa-moon','fa-solid fa-compass'][i%4]}/><span>{x}</span></li>)}</ul></div></div></div></main>}

function Journal({site}:{site:AuthorWebsite}){const posts=site.posts.filter(p=>p.published).slice(0,3);return <section id="journal" className={`border-t border-[var(--line)] ${SPACE[site.sectionSpacing]}`}><div className="mx-auto max-w-[1180px] px-5 md:px-8"><h2 className="text-3xl font-normal md:text-4xl">From the Journal</h2>{posts.length?<div className="mt-6 grid gap-4 md:grid-cols-3">{posts.map(p=><article key={p.id} className="rounded-xl border border-[var(--line)] bg-[color:var(--surface)] p-6"><time className="font-sans text-[10px] uppercase tracking-[.15em] text-[var(--accent)]">{p.date}</time><h3 className="mt-3 text-xl">{p.title}</h3><p className="mt-3 font-sans text-sm leading-6 text-[var(--muted)]">{p.excerpt}</p><a href={safeWebsiteUrl(p.url||'')||'#'} className="mt-5 inline-block text-sm text-[var(--accent)]">Read more →</a></article>)}</div>:<p className="mt-4 font-sans text-sm text-[var(--muted)]">Published posts will appear here.</p>}</div></section>}

function Footer({site,socials}:{site:AuthorWebsite;socials:AuthorWebsite['links']}){return <footer id="contact" className="border-t border-[var(--line)] bg-[color:var(--surface)]"><div className="mx-auto flex max-w-[1180px] flex-col gap-5 px-5 py-7 font-sans text-xs text-[var(--muted)] md:flex-row md:items-center md:px-8"><div className="flex items-center gap-3"><span className="text-3xl text-[var(--accent)]">☾</span><strong className="font-serif text-lg font-normal">{site.authorName||site.title}</strong></div><p className="md:mx-auto md:italic">{site.heroQuote||site.tagline}</p><div className="flex flex-wrap items-center gap-4">{socials.map(l=><a key={l.id} href={safeWebsiteUrl(l.url)} aria-label={l.label}><Icon icon={icon(l.kind)}/></a>)}{safeWebsiteUrl(site.privacyUrl)&&<a href={safeWebsiteUrl(site.privacyUrl)}>Privacy</a>}{safeWebsiteUrl(site.contactUrl)&&<a href={safeWebsiteUrl(site.contactUrl)}>Contact</a>}</div></div>{site.showMoonScribe&&<div className="border-t border-[var(--line)] py-3 text-center font-sans text-[10px] tracking-[.18em] text-[var(--muted)]">MADE WITH <span className="text-[var(--accent)]">MOONSCRIBE</span></div>}</footer>}
